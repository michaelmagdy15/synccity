import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // Replace with an identifier owned by your Apple developer team before registration.
  appId: 'com.synccity.game',
  appName: 'SyncCity',
  webDir: 'dist',
  // Release assets are bundled. Do not add a development server.url to release builds.
};

export default config;
